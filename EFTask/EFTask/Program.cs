using EFTask.Models;
using EFTask.Services;

namespace EFTask
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //PizzaService pizzaService = new PizzaService();
            //IngredientService ingredientService = new IngredientService();
            //SizeService sizeService = new SizeService();
            //pizzaService.CreatePizza("Pepperoni");
            //pizzaService.CreatePizza("Vegetarian");
            //pizzaService.CreatePizza("Mexicano");

            //ingredientService.CreateIngredient("Sausage", 1);
            //ingredientService.CreateIngredient("Mushroom", 1);
            //ingredientService.CreateIngredient("Cheese", 2);

            //sizeService.CreateSize("Small", 1, 12.50d);
            //sizeService.CreateSize("Medium", 1, 18.50d);
            //sizeService.CreateSize("Large", 2, 28.50d);

            //foreach (Pizza item in pizzaService.GetAll())
            //{
            //    Console.WriteLine(item.Name);
            //}
            //pizzaService.GetById(10);
            //pizzaService.Remove(1);

            //Pizza pizza = pizzaService.GetById(2);
            //pizza.Name = "Margarita";
            //pizzaService.Update(pizza);

            //foreach (Ingredient item in ingredientService.GetAll())
            //{
            //    Console.WriteLine("Id: " + item.Id +" Ingredient: " + item.Name + " Pizza: " + item.Pizza.Name);
            //}
        }
    }
}